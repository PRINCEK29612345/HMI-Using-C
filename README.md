# HMI-Using-C
An dynamic GUI in linux using gcc and os x11 library
Install gcc on your linux pc by running `sudo apt-get install gcc`
Then install the x11 library (if its not available) by running `sudo pat-get install libx11-dev`
Compile the code with x11 (gcc f_name.c -o exe_name -lX11)
